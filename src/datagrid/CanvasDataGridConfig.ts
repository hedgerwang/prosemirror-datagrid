//

export default class CanvasDataGridConfig {
  // Style.
  fixedColBGColor = '#f0f0f0';
  fixedRowBGColor = '#dedede';
  shadowBlur = 10;
  shadowColor = '#444';
  textColor = '#222';
  textSize = 12;
  cellPadding = 6;

  // Others
  fixedColsCount = 1;
  fixedRowsCount = 1;
  indexColumnWidth = 60;
  snapToGrid = false;

  // DEV
  devMode = true;
}
